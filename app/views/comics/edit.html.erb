<div class="comic-edit-form">
  <%= render 'form', comic: @comic %>
</div>

<%= link_to 'Back', comic_path(@comic, {:issue => @comic.issue, :volume => @comic.volume}), :class => "back-to-comic"%>

<%= link_to 'Show', comic_path(@comic, {:issue => @comic.issue, :volume => @comic.volume}) %> |
<%= link_to 'Back', comics_path({volume: @comic.volume}) %>
