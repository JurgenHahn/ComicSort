<table class="table table-striped table-bordered  table-hover">
  <thead>
    <tr>
      <th>Title</th>
      <th>Volume</th>
      <th>Issue</th>
      <th>Price</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @comics.each do |comic| %>
      <tr>
        <td><%= comic.title %></td>
        <td><%= comic.volume %></td>
        <td><%= number_with_precision(comic.issue, strip_insignificant_zeros: true)%></td>
        <td>$<%= comic.price %></td>
        <td><%= link_to 'Show', comic_path(comic, {:issue => comic.issue, :volume => comic.volume}), :class => "btn btn-default btn-xs" %></td>
    <% end %>
  </tbody>
</table>

<br>
